# CLI/MCP: the only model credential required. Export to the host process environment.
TYPESAFE_API_KEY=
# Optional: override native helper location.
# JEV_DRIVER_PATH=/absolute/path/to/desktop-driver
